
export const ReadList = () => {
  return (
    <div className='grid grid-cols-2 md:grid-cols-2 gap-4 p-4 m-auto w-[80%]'>
        {/* {books.map((book, index) => (
            <BooksCard
                key={'Card-' + index}
                classCus='bg-cover bg-center h-[250px] w-[80%] rounded-md cursor-pointer'
                cover={book.cover}
                synopsis={book.synopsis}
            />
        ))} */}
    </div>
  )
}
